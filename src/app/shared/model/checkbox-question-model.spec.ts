/* tslint:disable:no-unused-variable */

import {  async, inject } from '@angular/core/testing';
import {CheckboxQuestionModel} from './checkbox-question-model';

describe('CheckboxQuestionModel', () => {
  it('should create an instance', () => {
    expect(new CheckboxQuestionModel()).toBeTruthy();
  });
});
